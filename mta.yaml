---
_schema-version: '3.1'
ID: study
version: 1.0.0
description: "study Microservice"
parameters:
  enable-parallel-deployments: true

modules:
  - name: study
    type: nodejs
    path: srv
    parameters:
      buildpack: nodejs_buildpack
      keep-existing-routes: true
      disk-quota: 512M
      memory: 128M
    build-parameters:
      builder: npm
      ignore:
        - default-*.json
        - "*cache*"
        - "*node_modules*"
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: autoscaler-microservices
        

resources:
  - name: autoscaler-microservices
    type: org.cloudfoundry.managed-service
    parameters:
      service: autoscaler
      service-plan: standard
